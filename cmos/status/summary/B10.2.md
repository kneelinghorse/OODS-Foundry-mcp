Outcome: HTTP bridge for Storybook (read-only)

- Delivered `@oods/mcp-bridge` Fastify server with strict CORS.
- Endpoints: GET /health, GET /tools, POST /run (read-only).
- Bridges to `@oods/mcp-server` via stdio; forces `apply=false`.
- Returns normalized artifact paths for Storybook consumption.

