{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UI Status Map Configuration",
  "description": "Defines how UI contexts consume canonical enum→token mappings.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "generated_at", "domains", "contexts"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional reference to the schema used to validate this configuration."
    },
    "version": {
      "type": "string",
      "description": "Semantic version for the configuration document."
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp recording when the mapping was produced."
    },
    "notes": {
      "type": "string",
      "description": "Human-readable context for the current mapping version."
    },
    "domains": {
      "type": "object",
      "description": "Status domains that are backed by canonical token manifests.",
      "minProperties": 1,
      "additionalProperties": false,
      "patternProperties": {
        "^[a-z0-9_\\-]+$": {
          "type": "object",
          "additionalProperties": false,
          "required": ["source", "path", "statuses"],
          "properties": {
            "source": {
              "type": "string",
              "description": "Relative path to the token manifest providing status token payloads."
            },
            "path": {
              "type": "string",
              "pattern": "^#/",
              "description": "JSON Pointer into the source manifest for this domain."
            },
            "statuses": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9_\\-]+$"
              },
              "description": "Ordered canonical statuses served by the mapping."
            }
          }
        }
      }
    },
    "contexts": {
      "type": "object",
      "description": "UI contexts and the domains they render via the mapping.",
      "minProperties": 1,
      "additionalProperties": false,
      "patternProperties": {
        "^[a-z0-9_\\-]+$": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9_\\-]+$"
          }
        }
      }
    }
  }
}
