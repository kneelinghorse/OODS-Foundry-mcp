import { Badge, Button, Card, Text } from './base/index.js';
import { PageHeader } from './page/index.js';

export const OODS = {
  Button,
  Badge,
  Text,
  Card,
  PageHeader,
} as const;

export { Badge, Button, Card, Text, PageHeader };
