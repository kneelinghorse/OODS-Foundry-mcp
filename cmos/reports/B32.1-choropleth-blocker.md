# B32.1 ChoroplethMap – Blocker Report

**Status:** ✅ RESOLVED
**Date:** 2025-11-29
**Mission:** B32.1 ChoroplethMap Component
**Owner:** assistant

## Root Cause

**Token prefix mismatch**: Components referenced `--viz-scale-sequential-*` but the built CSS uses `--oods-viz-scale-sequential-*` (with `oods-` prefix).

This caused CSS variables to be unresolved, resulting in `black` fills in Storybook.

## Resolution

Updated all token references to use the correct `--oods-` prefix:

1. **ChoroplethMap.tsx** (line 220-225): `DEFAULT_COLOR_RANGE` updated
2. **ChoroplethMap.stories.tsx**: `BLUE_RANGE` and inline legend colors updated
3. **docs/components/choropleth-map.md**: All example token references updated

## Verification

- ✅ 25/25 tests passing (unit + a11y)
- ✅ Token CSS loads in Storybook (`packages/tokens/dist/css/tokens.css`)
- ✅ `--oods-viz-scale-sequential-*` tokens exist in built CSS

## Next Steps

1. Restart Storybook to confirm visual fix
2. Mark B32.1 as Completed
3. Proceed to B32.2 (BubbleMap Component)
