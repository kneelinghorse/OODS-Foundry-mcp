{
  "name": "@oods/artifacts",
  "version": "0.1.0",
  "private": true,
  "description": "Artifact writer and verifier for MCP tooling, CLI transcripts, and diagnostics.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "workspace:*",
    "rimraf": "^5.0.7",
    "typescript": "workspace:*",
    "vitest": "workspace:*"
  }
}
