{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "brand": {
      "type": "string",
      "enum": ["A"],
      "default": "A",
      "description": "Target brand identifier."
    },
    "delta": {
      "description": "Alias changes (object) or RFC 6902 patch array when strategy=patch.",
      "oneOf": [
        { "type": "object" },
        {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "op": { "type": "string", "enum": ["add", "remove", "replace"] },
              "path": { "type": "string" },
              "value": {}
            },
            "required": ["op", "path"],
            "additionalProperties": false
          }
        }
      ]
    },
    "strategy": {
      "type": "string",
      "enum": ["alias", "patch"],
      "default": "alias",
      "description": "Alias strategy rewrites token values; patch applies RFC 6902 operations."
    },
    "preview": {
      "type": "object",
      "description": "Preview response controls (apply=false).",
      "properties": {
        "verbosity": {
          "type": "string",
          "enum": ["full", "compact"],
          "default": "full",
          "description": "full includes structured before/after payloads; compact returns summary + hunks only."
        }
      },
      "additionalProperties": false
    },
    "apply": {
      "type": "boolean",
      "default": false
    }
  },
  "required": ["delta"],
  "allOf": [
    {
      "if": {
        "properties": { "strategy": { "const": "patch" } },
        "required": ["strategy"]
      },
      "then": {
        "properties": {
          "delta": {
            "type": "array"
          }
        }
      },
      "else": {
        "properties": {
          "delta": {
            "type": "object"
          }
        }
      }
    }
  ],
  "additionalProperties": false
}
